A machine learning classifier was trained using the extracted DSP features.

Baseline

The initial baseline model used [insert model name here, e.g. Random Forest / SVM / XGBoost / Logistic Regression] with standard DSP features.

Final Model

The final selected model was [insert final model name]. This model was chosen because it gave the best balance between:

validation accuracy
robustness across distorted conditions
stability across experiments
Hyperparameters

The final model used:

[insert parameter 1]
[insert parameter 2]
[insert parameter 3]
